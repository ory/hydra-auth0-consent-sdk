{
  "devDependencies": {
    "lerna": "^2.0.0",
    "npm-run-all": "^4.1.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:auth0": "npm-run-all build start:auth0",
    "serve:auth0": "lerna run --scope auth0-example start",
    "build": "lerna run build",
    "postinstall": "lerna bootstrap",
    "clean": "lerna clean"
  }
}
